{
  "name": "@splash/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy && pnpm apply-sql",
    "studio": "prisma studio",
    "reset": "prisma migrate reset --force && pnpm apply-sql && pnpm seed",
    "apply-sql": "tsx scripts/apply-sql.ts",
    "seed": "tsx prisma/seed.ts",
    "create-admin": "tsx scripts/create-admin.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "argon2": "^0.41.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "dotenv": "^16.4.5"
  }
}
